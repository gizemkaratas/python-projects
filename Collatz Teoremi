n=[]

a=int(input("bir sayı giriniz:"))
if a>0:        
    while a!=1:
           if a%2==0:
              a=a/2
              n.append(a)
           elif a%2 != 0:
              a = 3*a+1
              n.append(a)
    print(len(n))
else:
    print("negatif sayı girdiniz")
